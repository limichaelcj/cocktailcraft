<%= form_with url: action, method: method ||= :get, local: true, class: "ui #{'transparent' if local_assigns[:transparent]} left icon input #{classes if local_assigns[:classes]}" do |f| %>
  <i class="search icon"></i>
  <%= f.text_field (param ||= :search), placeholder: placeholder ||= 'Search' %>
<% end %>
