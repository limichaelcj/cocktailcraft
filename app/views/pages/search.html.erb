<div class="ui container">
  <div class="ui segment">
    <% if @results.any? %>
      <div class="ui very relaxed divided list">
        <% @results.each do |cocktail| %>
          <%= link_to cocktail_path(cocktail), class: 'item' do %>
            <div class="header"><%= cocktail.name %></div>
            <p><%= cocktail.description %></p>
            <% cocktail.doses.select { |dose| dose.ingredient.name.include? @query }.each do |dose| %>
              <div class="ui label"><%= dose.ingredient.name %></div>
            <% end %>
          <% end %>
        <% end %>
      </div>
    <% else %>
      No results.
    <% end %>
  </div>
</div>
